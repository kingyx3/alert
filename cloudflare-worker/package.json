{
  "name": "lazada-tcg-restock-monitor",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "check": "node --check src/index.js && node --check src/entry.js && wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "4.129.0"
  }
}
